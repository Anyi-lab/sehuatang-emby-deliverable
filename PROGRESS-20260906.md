# PROGRESS — avdb 油猴脚本排查(2026-09-06 存档)

## 已解决: "该条磁力获取不到链接" 根因与修复 (v0.3.3, commit d117f96, 2026-09-07)
### 根因 (detailState 竞态)
- `injectDetailButtons()` 原代码在 `await loadDetailData(movieId)` 之前就先把
  `detailState.movieId = movieId` 赋值; MutationObserver 在 await 挂起期间再次触发
  注入时, 因 `detailState.movieId === movieId` 判定"已加载"而跳过加载, 用**空的
  detailState** (magnets/resources 空数组) 注入按钮, 闭包捕获
  `cand = matchOnlineMagnetByCard(card, [], i) = null`; 且 btnInCard(card) 去重导致
  数据加载完成后不重注。同理 st.articles[i] 为空 -> 资源库磁力也 null。
### 修复
1. 新增 `detailLoadPromise` + `ensureDetailData(movieId)`: 并发触发共用同一 promise,
   data 就绪后再 Object.assign 到 detailState。
2. `injectDetailButtons()` 开头 `const st = await ensureDetailData(movieId)`, 数据就绪
   后再注入按钮。
3. 在线磁链 + 资源库磁力按钮回调改为**点击时实时 `await ensureDetailData(movieId)`
   重新取数据匹配** (不再捕获注入时的固定 cand/art 快照)。
4. 版本 0.3.2 -> 0.3.3, 初始化日志同步。
### 验证 (Tampermonkey 实际运行, 2026-09-07)
- 已部署到 Tampermonkey (uuid 58e1ceb2-cf23-48b0-8fdd-c78c79b55efa), verify 通过
  (30960 字节, 含 "0.3.3" + "ensureDetailData")。
- playback 页 (gyz5kg/MIKR-018) 三类卡片按钮点击全部弹出 "推送到 Emby 入库" 对话框,
  显示 **"磁力已就绪"** (不再报 "✗ 无磁力"):
  1. 在线磁链资源 (javdb 源, "中字mikr-018..." 2.39GB) → thread_id=gyz5kg
  2. 资源库磁力 (article 源, "MIKR-018 [无码破解]..." 6.88GB) → thread_id=2915681
  3. 评论区资源 ED2K (www.98T.la@MIKR-018.restored... 32.43GB) → 磁力已就绪
- 已 push GitHub (730953e..d117f96) + 桌面副本已同步。

## 油猴脚本 UUID (重要!)
- Tampermonkey 中脚本 UUID: `58e1ceb2-cf23-48b0-8fdd-c78c79b55efa`
- 从 dashboard 行内 span id 提取: `span_<base64(uuid)>_sname`, 本例
  `span_NThlMWNlYjItY2YyMy00OGIwLThmZGQtYzc4Yzc5YjU1ZWZhX3NuYW1l`
- tm_cdp.py 用法: open-editor <uuid> → list-scripts (找 CodeMirror, marker=脚本名)
  → inject <marker> <file.js> → save <base64(uuid)> → verify <marker> <needle...>
- dashboard 渲染有延迟: eval 查询需等待几秒, 否则显示 "没有安装任何脚本" 假象。

## 背景
- 环境: WSL2 + Windows10, avdb Docker 容器(端口 8200, article 表 32.7 万行)
- 仓库: /root/clacky_workspace/sehuatang-emby-deliverable (GitHub: Anyi-lab/sehuatang-emby-deliverable)
- 主脚本: avdb-emby-inject.user.js (桌面副本 /mnt/d/Desktop/avdb-emby-inject.user.js)
- 浏览器工具: Edge 远程调试端口 9333 (socat 桥接 127.0.0.1:9333 -> 172.25.224.1:9333,
  portproxy 0.0.0.0:9333 -> 127.0.0.1:9333, DevToolsActivePort 在
  /mnt/c/Users/24846/AppData/Local/Microsoft/Edge/User Data/DevToolsActivePort)

## 已修复: playback 页 "无磁力" (v0.3.2, commit 730953e, 已 push + 桌面副本已同步)
- 根因: playback 页磁力数据源 = javdb magnets 接口 + javdb comment-resources 接口。
  gyz5kg (MIKR-018) 标准磁力为空, 3 条磁力全部来自评论区资源
  (javdb_comment_resource 表: 2 条 ed2k + 1 条 magnet, comment_date desc 排序)。
- 改动:
  1. loadDetailData() 新增 2b 步: 拉 /api/v1/javdb/movies/{id}/comment-resources 存 st.resources
  2. 新增 onlineMagnetCandidates(st): 合并 magnets+resources 成 {href,name,key} (过滤 magnet:/ed2k: 前缀)
  3. 新增 matchOnlineMagnetByCard(card,candidates,i): 三级匹配 (卡片文本含 hash -> 文件名包含 -> 按序 fallback)
  4. injectDetailButtons() 在线磁链部分改用上述匹配
  5. 版本 0.3.1 -> 0.3.2, 描述更新
- 验证: node --check OK; 模拟 gyz5kg 数据测试匹配全部通过 (含顺序打乱)。

## 仍然存在的问题
- 用户更新到 v0.3.2 后仍报 toast "入库失败: 该条磁力获取不到链接"
  (makeDetailBtn line 584, getMagnet() 返回 null 时触发)。
- 已确认: 评论资源接口后端正常 (查库返回 3 条); import_api(5081) 支持 magnet 和 ed2k。
- 排查断点: 浏览器里 st.resources 是否拉取成功 (401/网络/缓存)、detailState 首次加载失败被缓存、
  或用户实际点的页面/卡片不同。需要在页面 console 验证 st.resources / detailState。

## 前端代码确认 (容器内 grep 压缩 JS)
- playback 磁力区组件 bo({magnets, commentResources, isLoading,...}):
  section 标题 "在线磁链资源", 空状态 "暂无在线磁链资源"。
- 磁力卡片与评论区资源卡片 className 均为
  `flex flex-col gap-3 rounded-xl border bg-muted/20 p-3 sm:flex-row sm:items-center sm:justify-between`
  -> 与脚本 cardsInSection 选择器 (div.rounded-xl.border + bg-muted/20 + p-3) 匹配。
- sectionByTitle('在线磁链') 用 startsWith 匹配 "在线磁链资源" 成功。

## 浏览器调试实例 (重要!)
- **管理脚本**: /root/clacky_workspace/edge_debug_restart.sh (edge-debug-launcher skill 载体)
  - 幂等启动: bash edge_debug_restart.sh
  - 强制重启: bash edge_debug_restart.sh --force
  - 只查状态: bash edge_debug_restart.sh --status
- profile 固定: F:\WSL\TEMP\edge-cd2-debug (全新干净, 仅解压版油猴,
  --load-extension=F:\WSL\TEMP\F-WSL-Android\edge-tampermonkey\5.5.0_0)
- 端口 9333, Edge 152.0.4191.62。

### ⛔ 教训 (2026-09-06, 必须遵守)
- **禁止绕过 edge-debug-launcher skill 手动杀 Edge / 自建临时 profile**。
  曾用 Stop-Process -Name msedge -Force 杀掉 9333 调试实例, 并自建
  C:\Temp\edge-debug-avdb (9222) 临时 profile —— 错误操作, 用户批评。
- **禁止在 C 盘创建任何 Edge/Chrome profile**, --user-data-dir 必须 F:\ 开头。
  C:\Temp\edge-debug-avdb (363M) 已清理: 迁移至 F:\WSL\clacky-files\backup\edge-debug-avdb,
  C 盘已无残留。
- 浏览器调试相关 skill:
  - edge-debug-launcher: 9333 调试实例启停 (等价 edge_debug_restart.sh)
  - tampermonkey-cdp-edit: 通过 CDP 编辑/部署油猴脚本
  - c-drive-browser-profile-cleanup: 清理 C 盘调试 profile 残留
  - browser-setup: 默认配置

## 当前状态 (2026-09-07 保存时刻)
- **v0.3.3 已部署到 Tampermonkey 并验证**: 三类卡片按钮全部 "磁力已就绪", 修复完成。
- **v0.3.4 (commit 9563655) 代码审查加固并部署验证通过**:
  - numberFromCard 增加卡片全文搜番号兜底 (番号与其他文字同行时也能识别)
  - matchOnlineMagnetByCard fallback 环取 (i % len), 防卡片数 > 候选数越界取 undefined
  - ensureDetailData 跨页 movieId 校验, 防详情页 A→B 切换时旧按钮加载覆盖 detailState
  - pollStatus 连续 10 次无响应 (30s) 判失败, 不再无限循环卡按钮 3 小时
  - import_api 地址按 location.hostname 推断 (局域网 IP 访问时同 host:5081)
  - 实机验证: playback 页 61~77 个按钮注入, 点击弹窗 "磁力已就绪" (SDMU-993/javdb)
- 9333 调试实例: 正常 (Edg/152.0.4191.66)。
- 已 push GitHub (9563655) + 桌面副本已同步。

## 下一步 (待用户指示)
1. 用户可在自己浏览器更新到 v0.3.4 (GitHub raw @updateURL), 实际走一次完整入库流程
   (选分类 → 开始入库 → import_api 推送 115), 确认末端链路。
2. 如有其他页面 (ranking/top/latest 卡片、文章卡片) 按钮问题, 按同样模式排查。